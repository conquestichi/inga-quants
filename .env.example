# inga-quant environment variables
# Copy to .env and fill in real values. NEVER commit .env to git.

# J-Quants API credentials
JQUANTS_MAIL_ADDRESS=your-email@example.com
JQUANTS_PASSWORD=your-password

# Slack Webhook URL for daily reports
# If unset, output is saved to output/<trade_date>/slack_payload.json
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# Optional overrides
# INGA_CONFIG=config/config.yaml
# INGA_LOG_LEVEL=INFO
