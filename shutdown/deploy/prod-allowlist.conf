# Production unit allowlist for inga-prod-apply
# ──────────────────────────────────────────────────────────────────────────────
# Units listed here will be:
#   - enabled + started (systemctl enable --now)
#   - checked for failed state at the end of inga-prod-apply
#
# These units MUST exit 0 for operational SKIP conditions:
#   non_trading_day / api_key_missing / no_data → [SKIP] + exit 0
#
# To discover all inga units on the VPS:
#   systemctl list-timers --all | grep inga
#   systemctl list-units 'inga-*' --all
#
# One unit name per line. Comments (#) and blank lines are ignored.
# ──────────────────────────────────────────────────────────────────────────────

# ── J-Quants daily bar ingest ─────────────────────────────────────────────────
inga-market-quotes-ingest.service

# ── Universe 300 weekly refresh ───────────────────────────────────────────────
inga-universe300-build.service

# ── Weekly KPI digest ─────────────────────────────────────────────────────────
inga-weekly-digest.service

# ── Add other production units below ─────────────────────────────────────────
# inga-signals.service
# facts-pack-v2.service
