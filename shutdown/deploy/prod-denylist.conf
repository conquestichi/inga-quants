# Production unit denylist for inga-prod-apply
# ──────────────────────────────────────────────────────────────────────────────
# Units listed here will be:
#   - stopped + disabled (systemctl disable --now)
#   - masked (systemctl mask) — cannot start accidentally
#
# Use this for retired / legacy units that keep appearing in systemctl --failed.
# "If we don't run it, we don't want it in --failed."
#
# To find candidates:
#   systemctl --failed
#   systemctl list-units --state=failed
#
# One unit name per line. Comments (#) and blank lines are ignored.
# ──────────────────────────────────────────────────────────────────────────────

# ── Legacy / retired units (uncomment as needed) ──────────────────────────────
# ink_metrics_export.service
# inkaritsu-autopush.service
# inkaritsu-autopush.timer
